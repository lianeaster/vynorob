# SendGrid Email Configuration
# Get your API key from: https://app.sendgrid.com/settings/api_keys
#
# Locally you can use sendgrid.env (same keys; supports export KEY='value' lines).
# On the server, systemd EnvironmentFile often points at a file like this (KEY=value, no export).

SENDGRID_API_KEY=SG.your-api-key-here
SENDER_EMAIL=your-verified-email@example.com

# Setup instructions: See SENDGRID_SETUP.md
